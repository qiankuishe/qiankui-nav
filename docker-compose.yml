version: '3.8'

services:
  daohang:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - daohang-data:/app/data
    environment:
      - JWT_SECRET=your-secret-key-change-me
      - PORT=3000
    restart: unless-stopped

volumes:
  daohang-data:
